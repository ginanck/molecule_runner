# Python 3.6 compatible requirements for Ansible Molecule testing
# Note: Python 3.6 support is very limited, using last known working versions

# Pin ansible-core version for consistent molecule testing (other packages auto-resolve)
ansible-core==2.11.12

# Pin compatible versions for Python 3.6
urllib3<1.27,>=1.25.4
requests>=2.25.1,<2.29.0
cryptography>=3.2,<41.0.0
charset-normalizer<3.0.0
