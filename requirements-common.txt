# Minimal Ansible ecosystem packages for Molecule testing
# Core Ansible and Molecule tools
ansible-compat
ansible-lint
docsible
flake8
yamllint
molecule
molecule-docker

# Essential Python packages for Ansible
# Note: cryptography, PyYAML, jinja2, packaging installed via Alpine packages

# Docker support
docker
requests
urllib3
